# maroonshift-gui uses only Python standard-library modules.
# No third-party dependencies to install via pip.
#
# System dependencies (must be installed on the host):
#   - python3 + python3-tk  (for tkinter)
#   - redshift              (screen color temperature adjustment)
#
# Optional (for XFCE4 panel integration):
#   - xfce4-panel + xfconf  (xfconf-query command)
